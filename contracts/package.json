{
  "name": "privabid-contracts",
  "version": "0.1.0",
  "description": "PrivaBid sealed-bid auction protocol — smart contracts",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:gas": "REPORT_GAS=true hardhat test",
    "deploy:arb": "hardhat run scripts/deploy.ts --network arb-sepolia",
    "deploy:eth": "hardhat run scripts/deploy.ts --network eth-sepolia",
    "deploy:base": "hardhat run scripts/deploy.ts --network base-sepolia",
    "typechain": "hardhat typechain",
    "clean": "hardhat clean"
  },
  "dependencies": {
    "@fhenixprotocol/cofhe-contracts": "^0.1.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@types/node": "^20.0.0",
    "cofhe-hardhat-plugin": "^0.1.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.0.0"
  }
}
